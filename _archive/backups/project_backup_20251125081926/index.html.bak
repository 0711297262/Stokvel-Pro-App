<!DOCTYPE html>
<html lang="en">
<head>
  <title>Welcome — Stokvel PRO</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Onboarding Styles -->
  <link rel="stylesheet" href="css/onboarding.css" />

  <!-- Theme (light/dark auto support) -->
  <script type="module" src="/js/theme.js"></script>

  <style>
    body {
      margin: 0;
      font-family: system-ui, -apple-system, "Inter", Arial, sans-serif;
      background: #f5f7fb;
      color: #1a1f36;
    }

    #onboardRoot {
      max-width: 480px;
      margin: 0 auto;
      padding: 32px 22px 80px;
    }

    h1 {
      font-size: 28px;
      font-weight: 800;
      margin-bottom: 12px;
    }

    h2 {
      font-size: 24px;
      font-weight: 700;
      margin-bottom: 8px;
    }

    p {
      color: #56627a;
      font-size: 15px;
      line-height: 1.5;
      margin-bottom: 20px;
    }

    .slide {
      display: none;
      animation: fade 0.3s ease-out;
    }

    .slide.active {
      display: block;
    }

    @keyframes fade {
      from { opacity: 0; transform: translateY(8px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .btn-primary {
      width: 100%;
      padding: 14px;
      border: none;
      border-radius: 12px;
      background: linear-gradient(to right, #2d7ff9, #528bff);
      color: white;
      font-weight: 700;
      cursor: pointer;
      margin-top: 16px;
      font-size: 16px;
      box-shadow: 0 4px 14px rgba(45, 127, 249, 0.25);
    }

    .btn-primary:active {
      transform: scale(0.98);
    }

    .btn-ghost {
      padding: 12px;
      width: 100%;
      border: 2px solid #d6d9e4;
      background: white;
      font-weight: 700;
      border-radius: 12px;
      color: #2d7ff9;
      margin-top: 14px;
      cursor: pointer;
    }

    .pager {
      display: flex;
      justify-content: center;
      gap: 8px;
      margin-top: 22px;
    }

    .dot {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background: #d6d9e4;
    }

    .dot.active {
      background: #2d7ff9;
    }
  </style>
</head>

<body>
<main id="onboardRoot">

  <!-- Slide 1 -->
  <section class="slide active" data-index="1">
    <h1>Welcome to Stokvel PRO</h1>
    <p>The simplest way to manage contributions, payouts, members, and activity for your stokvel.</p>
    <button class="btn-primary next">Next</button>
  </section>

  <!-- Slide 2 -->
  <section class="slide" data-index="2">
    <h2>How it works</h2>
    <p>
      1. Create or join a stokvel<br>
      2. Record monthly contributions<br>
      3. Automate payouts & track history
    </p>
    <button class="btn-primary next">Next</button>
    <button class="btn-ghost back">Back</button>
  </section>

  <!-- Slide 3 -->
  <section class="slide" data-index="3">
    <h2>Privacy & Security</h2>
    <p>Your data stays private — we don't sell or share it. Admin approvals required for critical actions.</p>
    <button class="btn-primary next">Next</button>
    <button class="btn-ghost back">Back</button>
  </section>

  <!-- Slide 4 -->
  <section class="slide" data-index="4">
    <h2>Ready to start?</h2>
    <p>Create a stokvel or join using a code. Your subscription is R39/month — cancel anytime.</p>
    <button id="getStartedBtn" class="btn-primary">Get Started</button>
    <button class="btn-ghost back">Back</button>
  </section>

  <!-- Progress dots -->
  <nav class="pager">
    <div class="dot active"></div>
    <div class="dot"></div>
    <div class="dot"></div>
    <div class="dot"></div>
  </nav>

</main>

<!-- Onboarding Logic -->
<script type="module" src="js/onboarding.js"></script>

</body>
</html>
