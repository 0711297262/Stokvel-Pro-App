<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Component Gallery v3 — Stokvel PRO</title>

  <style>
    body {
      margin: 0;
      padding: 40px;
      font-family: system-ui, -apple-system, "Segoe UI", Roboto;
      background: #f6f9fc;
      color: #1e2a36;
    }

    h1 {
      font-size: 32px;
      font-weight: 800;
      margin-bottom: 6px;
    }
    h2 {
      margin-top: 32px;
      font-weight: 700;
      opacity: 0.85;
    }
    .section {
      margin-top: 24px;
      padding: 20px;
      background: white;
      border-radius: 16px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.06);
    }

    /* Cards */
    .card {
      background: white;
      border-radius: 18px;
      padding: 20px;
      box-shadow: 0 8px 28px rgba(0,0,0,0.08);
      margin-bottom: 20px;
    }

    /* Buttons */
    .btn {
      padding: 12px 16px;
      border-radius: 12px;
      border: none;
      cursor:pointer;
      transition:0.2s;
      font-size:15px;
      font-weight:600;
    }
    .btn-primary {
      background: linear-gradient(90deg, #2b7bff, #5dabff);
      color:white;
    }
    .btn-primary:hover {
      transform:translateY(-3px);
      box-shadow:0 6px 18px rgba(0,0,0,0.15);
    }

    /* KPIs */
    .kpi-box {
      background:white;
      border-radius:16px;
      padding:18px;
      width:220px;
      box-shadow:0 4px 14px rgba(0,0,0,0.05);
      text-align:center;
      display:inline-block;
      margin-right:16px;
    }
    .kpi-box .value {
      font-size:28px;
      font-weight:700;
      margin-top:4px;
    }

    /* Glass Nav preview */
    .nav-preview {
      margin-top:40px;
      background:rgba(255,255,255,0.55);
      backdrop-filter:blur(14px) saturate(160%);
      border-radius:24px;
      padding:20px;
      display:flex;
      justify-content:space-between;
      box-shadow:0 8px 32px rgba(15,25,40,0.12);
    }

    .nav-item {
      display:flex;
      flex-direction:column;
      align-items:center;
      font-size:13px;
      opacity:0.85;
    }
  </style>
</head>

<body>
  <div id="headerContainer"></div>
  <script src="/devtools/devtools_loader.js" defer></script>
  <script src="/devtools/use_real_theme.js" defer></script>
  <script src="/devtools/motion_presets.js"></script>


  <h1>Component Gallery v3</h1>
  <p>Premium UI system — Stokvel PRO 2025 Edition</p>

  <hr style="margin: 26px 0; opacity: 0.3;" />

  <!-- Buttons section -->
  <h2>Buttons</h2>
  <div class="section">
    <button class="btn btn-primary">Primary Button</button>
  </div>

  <!-- KPI Cards -->
  <h2>KPI Cards</h2>
  <div class="section">
    <div class="kpi-box">
      <div>Total Groups</div>
      <div class="value">6</div>
    </div>

    <div class="kpi-box">
      <div>Monthly Contributions</div>
      <div class="value">R 350</div>
    </div>
  </div>

  <!-- Cards -->
  <h2>Cards</h2>
  <div class="section">
    <div class="card">Standard Card Component</div>
  </div>

  <!-- Glass Navigation Bar -->
  <h2>Glass Navigation Preview</h2>
  <div class="nav-preview">
    <div class="nav-item">🏠<br>Home</div>
    <div class="nav-item">➕<br>Create</div>
    <div class="nav-item">🔔<br>Alerts</div>
    <div class="nav-item">👤<br>Profile</div>
  </div>

  <div id="footerContainer"></div>
  <div id="bottomNavContainer"></div>
  <script type="module" src="/js/load-partials.js"></script>
</body>
</html>
